/**
 * @forgekit/app-shared — entry point.
 * Re-exports the Telegram BotApi and update routing helpers so apps can
 * import everything from the package root (subpath exports also available).
 */
export * from "./botapi";
export * from "./updates";
